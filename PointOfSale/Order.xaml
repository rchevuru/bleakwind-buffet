<UserControl x:Class="PointOfSale.Order"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition />
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            

            <RowDefinition/>
            <!--<RowDefinition Height="60"/> -->
            <!--<RowDefinition Height="60"/> -->
            <!---<RowDefinition Height="60"/>-->
        </Grid.RowDefinitions>
        <ListView x:Name="OrderItemView"
                      ItemsSource="{Binding}"
                      SelectionChanged ="OrderItemView_SelectionChanged"
                  HorizontalContentAlignment="Stretch"
                      ScrollViewer.VerticalScrollBarVisibility="Visible"
                      ScrollViewer.HorizontalScrollBarVisibility="Hidden" Grid.RowSpan="4" HorizontalAlignment="Center" Width="400">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>

                        <TextBlock Grid.Row="1" HorizontalAlignment="Left" x:Name="ToString" Text="{Binding Path=ToString}" />
                        <TextBlock Grid.Row="1" HorizontalAlignment="Right"  x:Name="Price" Text="{Binding Path=Price}" />
                        <ListView x:Name="SpecialInstructionsView"
                                  ItemsSource="{Binding SpecialInstructions}"
                                  SelectionChanged="SpecialInstructionsView_SelectionChanged"
                                  HorizontalAlignment="Stretch"
                                  ScrollViewer.VerticalScrollBarVisibility="Visible" 
                                  ScrollViewer.HorizontalScrollBarVisibility="Visible" >
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=SpecialInstructions}" />
                                    <!--<Button>Remove from order</Button> -->
                                </DataTemplate>
                            </ListView.ItemTemplate>

                        </ListView>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Grid.Column ="1" Grid.Row ="0"  FontWeight="Black" TextAlignment="Right" FontSize="18" Text="{Binding Path=CountOrderNum}">Order</TextBlock>
        <TextBlock Grid.Column ="1" Grid.Row ="2" FontWeight="Black" TextAlignment="Right" FontSize="18" Text="{Binding Path=Tax}" >Tax</TextBlock>
        <TextBlock Grid.Column ="1" Grid.Row ="1" DockPanel.Dock="Bottom" FontWeight="Black" TextAlignment="Right" FontSize="18" Text="{Binding  Path=Subtotal}">Subtotal</TextBlock>
        <TextBlock Grid.Column ="1" Grid.Row ="3" FontWeight="Black" TextAlignment="Right" FontSize="18" Text="{Binding Path=Total}"> Total</TextBlock>
        <Button Grid.Row ="3" FontWeight="Black"  FontSize ="18" Click="NewOrder_Click" Grid.ColumnSpan="2" Margin="0,64,0,64" Grid.RowSpan="3" >New Order</Button>
        <Button Grid.Row ="5" FontWeight="Black" FontSize="18" Click="Cancel_Click" Grid.ColumnSpan="2">Cancel Order</Button>

    </Grid>
</UserControl>
